// Define Loading.js React component
// Import necessary modules
// use const instead of var so variable is static type
import React from 'react';
import './Loading.css';

const Loading = () => {
  return (
    <div className="Loading">
      Loading…
    </div>
  );
};

export default Loading;
